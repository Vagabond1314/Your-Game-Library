<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cvQuickGameRoot"
    android:layout_width="80dp"
    android:layout_height="110dp"
    android:layout_margin="4dp"
    app:cardBackgroundColor="#2A2A2A"
    app:cardCornerRadius="10dp"
    app:cardElevation="0dp"
    app:strokeWidth="0dp"
    app:strokeColor="#58A870"> <!-- Колір рамки при виборі -->

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/ivQuickGameCover"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/placeholder" />

        <!-- Напівпрозорий шар, який з'являється при виборі -->
        <View
            android:id="@+id/vSelectionOverlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#4058A870"
            android:visibility="gone" />

    </FrameLayout>
</com.google.android.material.card.MaterialCardView>